{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "Early Survival mode for Cosmic Reach!",
  "authors": [
  ],
  "license": "CC0-1.0",
  "environment": "*",
  "entrypoints": {
    "main": [
      "cc.badideas.survivalmode.SurvivalMode"
    ]
  },
  "mixins": [
    "survivalmode.mixins.json"
  ],
  "depends": {
    "cosmic_reach": ">=${cosmic_reach_version}",
    "fabricloader": ">=${loader_version}",
    "fluxapi": ">=0.4.1"
  }
}